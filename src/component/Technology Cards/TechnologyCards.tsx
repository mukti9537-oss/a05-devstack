import React, { use } from 'react';

const TechnologyCards = ({TechnologyCardsPromise}) => {
   const TechnologyCards = use(TechnologyCardsPromise);
   console.log(TechnologyCards, "TechnologyCards")
    return (
        <div>
            
        </div>
    );
};

export default TechnologyCards;